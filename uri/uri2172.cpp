#include <bits/stdc++.h>
using namespace std;
typedef unsigned long long nat;

int main() {
    nat x, y;
    while (cin >> x >> y, x || y) {
        cout << x * y << endl;
    }
    return 0;
}
